# 🎧 DLsite ASMR 管理工具 · 设计书

> 版本：v1.1  
> 作者：小玥喵 🐱  
> 日期：2026-08-02  
> 目标平台：🪟 Windows 10/11

---

## 📋 目录

1. [项目概述](#1-项目概述)
2. [技术栈选型](#2-技术栈选型)
3. [系统架构](#3-系统架构)
4. [功能模块](#4-功能模块)
5. [数据库设计](#5-数据库设计)
6. [DLsite API 集成](#6-dlsite-api-集成)
7. [UI/UX 设计](#7-uiux-设计)
8. [开发路线图](#8-开发路线图)
9. [部署与打包](#9-部署与打包)

---

## 1. 项目概述

### 1.1 目标

打造一个 **轻量、美观、跨平台** 的 DLsite 作品管理桌面端，聚焦 ASMR 音声作品的管理与播放，同时支持同人游戏、漫画等作品类型。

### 1.2 核心能力

| 能力 | 说明 |
|:----|:------|
| 🗂️ **作品库管理** | 多账号同步、本地导入、搜索筛选、标签分类 |
| ⬇️ **一键下载** | 从 DLsite 下载已购作品，支持断点续传 |
| 🎵 **本地播放** | 内置 ASMR 音频播放器，支持播放列表、进度记忆 |
| 🏷️ **元数据管理** | 自动抓取封面、社团、声优、标签等信息 |
| 🔄 **多端同步** | 通过 WebDAV / Syncthing 实现多设备库同步 |

### 1.3 目标用户

- DLsite 重度用户，购买了大量 ASMR 音声作品
- 需要统一管理、快速搜索、便捷播放的「老司机」
- 技术背景：有一定动手能力，能自己部署服务

---

## 2. 技术栈选型

### 2.1 推荐方案：Tauri 2.0（首选）

| 层级 | 技术 | 说明 |
|:---|:-----|:------|
| 🖥️ **桌面框架** | **Tauri 2.0** | Rust 驱动，Windows 上原生表现优秀，包体仅 ~10MB |
| 🎨 **前端框架** | **React / Vue 3 / Svelte** | 推荐 Vue 3（中文生态好）或 Svelte（最轻量） |
| 🦀 **后端核心** | **Rust** | 通过 `tauri::command` 暴露给前端调用 |
| 🗄️ **数据库** | **SQLite**（`rusqlite`） | 轻量嵌入，Windows 上无需额外安装 |
| 📦 **构建工具** | **Vite** | 极速 HMR，开发体验好 |
| 🎵 **音频播放** | **Rust 端：`rodio` / `cpal`** | 低延迟音频播放，Windows WASAPI 支持 |

### 2.2 备选方案：Electron（上手更快，不碰 Rust）

| 层级 | 技术 |
|:---|:-----|
| 🖥️ 桌面框架 | Electron 33+ |
| 🎨 前端 | React / Vue 3 |
| 🗄️ 数据库 | better-sqlite3 |
| 🎵 音频播放 | Howler.js / Web Audio API |

### 2.3 推荐理由（Windows 视角）

```
Tauri 优势（针对 Windows）：
  ✅ 打包体积 ~10MB（Electron ~150MB，多一个包管理不如Tauri清爽）
  ✅ 内存占用仅 Electron 的 1/3
  ✅ Rust 原生性能，Windows Native API 支持好
  ✅ 支持 Windows 10/11，可生成 .msi 安装包
  ✅ 支持系统托盘、全局快捷键等 Windows 原生功能

Tauri 劣势（针对 Windows）：
  ⚠️ 需要安装 Rust 工具链（rustup-init.exe 一键搞定）
  ⚠️ 需要安装 WebView2（Win10 自带，Win11 自带）
  ⚠️ 音频播放用 Rust 实现比 Web Audio 稍微复杂一点点

结论：强烈推荐 Tauri，值得花半天学一下 Rust 基础喵~！
```

---

## 3. 系统架构

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                    🖥️ 桌面客户端                          │
│  ┌──────────────────────────────────────────────────┐   │
│  │               🎨 前端层 (Svelte/Vue)              │   │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────────────┐   │   │
│  │  │ 作品库 │ │ 搜索  │ │ 播放器 │ │  设置/账号   │   │   │
│  │  │ 页面  │ │ 页面  │ │ 页面  │ │   页面       │   │   │
│  │  └──┬───┘ └──┬───┘ └──┬───┘ └──────┬───────┘   │   │
│  └─────┼─────────┼─────────┼────────────┼──────────┘   │
│        │         │         │            │               │
│  ┌─────┼─────────┼─────────┼────────────┼──────────┐   │
│  │   🦀 Rust 后端层 (Tauri Commands)                │   │
│  │  ┌────────┐ ┌────────┐ ┌────────┐ ┌──────────┐ │   │
│  │  │ api    │ │download│ │archive │ │storage   │ │   │
│  │  │ 模块   │ │ 模块   │ │ 模块   │ │ 模块     │ │   │
│  │  └───┬────┘ └───┬────┘ └───┬────┘ └────┬─────┘ │   │
│  └──────┼───────────┼──────────┼───────────┼───────┘   │
└─────────┼───────────┼──────────┼───────────┼───────────┘
          │           │          │           │
    ┌─────▼───┐ ┌────▼───┐  ┌──▼────┐ ┌────▼──────┐
    │DLsite   │ │ 本地   │  │压缩包 │ │ SQLite    │
    │Play API │ │文件系统 │  │解压   │ │ 数据库    │
    └─────────┘ └────────┘  └───────┘ └───────────┘
```

### 3.2 模块职责

| 模块 | 职责 | 技术 |
|:---|:-----|:-----|
| **api 模块** | DLsite Play v3 API 调用、认证、元数据抓取 | Rust (`reqwest`) |
| **download 模块** | 文件下载引擎，断点续传，进度回调 | Rust (`reqwest` + stream) |
| **archive 模块** | 压缩包识别、解压、文件结构分析 | Rust (`zip` / `tar` / `sevenz-rust`) |
| **storage 模块** | SQLite 数据库操作，CRUD，搜索 | Rust (`rusqlite` / `diesel`) |
| **player 模块** | 音频播放控制，播放列表，进度记忆 | Rust (`rodio` + ` Symphonia`) |
| **scanner 模块** | 本地文件夹扫描，RJ号识别，元数据匹配 | Rust |
| **credential 模块** | 账号凭证安全存储 | Rust 系统密钥链 |

### 3.3 数据流

```
📥 导入作品流程：

用户添加DLsite账号
       ↓
点击「同步」→ 调用 DLsite API 获取已购列表
       ↓
缓存元数据到 SQLite（作品ID、标题、社团、封面URL等）
       ↓
用户选择下载 → 下载引擎开始下载压缩包
       ↓
下载完成 → 解压 → 扫描音频文件 → 写入本地路径到数据库
       ↓
用户可在库中浏览、搜索、播放
```

---

## 4. 功能模块

### 4.1 账号管理

```
┌─────────────────────────────────────┐
│          📋 账号管理                  │
├─────────────────────────────────────┤
│  + 添加 DLsite 账号                  │
│     - 账号密码登录 (需要研究DLsite  │
│       的登录流程)                    │
│     - Cookie 登录 (更稳定)          │
│  + 多账号管理                        │
│     - 支持多个账号同时管理           │
│     - 同一作品多账号拥有去重         │
│  + 同步控制                          │
│     - 手动同步 / 自动同步           │
│     - 同步进度显示                   │
│  + 凭证安全存储                      │
│     - 使用系统密钥链                 │
│     - 不存储在 SQLite 中             │
└─────────────────────────────────────┘
```

### 4.2 作品库

```
┌─────────────────────────────────────┐
│          📚 作品库                    │
├─────────────────────────────────────┤
│  + 视图模式                          │
│     - 网格视图（封面缩略图）         │
│     - 列表视图（详细信息）           │
│  + 搜索与筛选                        │
│     - 关键词搜索（标题/RJ号）        │
│     - 按社团筛选                     │
│     - 按声优筛选                     │
│     - 按标签筛选                     │
│     - 按下载状态筛选                 │
│  + 排序                              │
│     - 按购买日期 / 标题 / 社团       │
│  + 批量操作                          │
│     - 批量下载 / 批量删除 / 批量打标签│
│  + 本地导入                          │
│     - 扫描指定文件夹，自动识别RJ号   │
│     - 匹配DLsite元数据               │
│     - 支持「仅本地」作品标记         │
└─────────────────────────────────────┘
```

### 4.3 作品详情

```
┌─────────────────────────────────────┐
│          📄 作品详情                  │
├─────────────────────────────────────┤
│  + 基本信息                          │
│     - 封面大图                        │
│     - 标题（中/日/英）               │
│     - RJ号（一键复制）               │
│     - 社团名 / 声优列表              │
│     - 购买日期 / 价格                │
│  + 元数据                            │
│     - 作品类型（音声/游戏/漫画等）    │
│     - 标签/分类                      │
│     - 作品简介/描述                  │
│  + 本地状态                          │
│     - 下载状态（未下载/已下载/下载中）│
│     - 本地路径                       │
│     - 文件大小/时长                   │
│  + 操作按钮                          │
│     - 打开 DLsite 页面               │
│     - 下载 / 重新下载                │
│     - 打开本地文件夹                  │
│     - 播放（音声作品）               │
│     - 编辑自定义标签                 │
│  + 自定义标签                        │
│     - 用户可以自由添加标签           │
│     - 标签颜色分类                   │
└─────────────────────────────────────┘
```

### 4.4 下载管理

```
┌─────────────────────────────────────┐
│          ⬇️ 下载管理                  │
├─────────────────────────────────────┤
│  + 下载队列                          │
│     - 当前下载任务列表               │
│     - 进度条 + 速度显示              │
│     - 暂停 / 继续 / 取消             │
│  + 断点续传                          │
│     - 临时文件缓存                   │
│     - 中断后自动恢复                 │
│  + 下载策略                          │
│     - 并发下载数控制                 │
│     - 下载目录设置                   │
│     - 下载完成后自动解压             │
│  + 历史记录                          │
│     - 已完成的下载列表               │
│     - 下载失败日志                   │
└─────────────────────────────────────┘
```

### 4.5 音频播放器（核心特色）

```
┌─────────────────────────────────────┐
│          🎵 ASMR 播放器               │
├─────────────────────────────────────┤
│  + 基本播放功能                      │
│     - 播放 / 暂停 / 上一首 / 下一首  │
│     - 进度条拖拽                     │
│     - 音量控制                       │
│     - 倍速播放（0.5x ~ 2.0x）        │
│  + 播放列表                          │
│     - 自动加载当前作品所有音轨       │
│     - 拖拽排序                       │
│     - 循环模式（单曲/列表/随机）     │
│  + 播放进度记忆                      │
│     - 自动保存每个作品的播放位置     │
│     - 下次打开时提示「继续播放」     │
│  + 迷你模式                          │
│     - 最小化到系统托盘               │
│     - 全局快捷键控制                 │
│     - 桌面歌词/浮动窗口              │
│  + 音频可视化                        │
│     - 波形图 / 频谱图                │
│     - 跟随播放实时跳动               │
└─────────────────────────────────────┘
```

### 4.6 标签与分类系统

```
┌─────────────────────────────────────┐
│          🏷️ 标签系统                  │
├─────────────────────────────────────┤
│  + 预置标签                          │
│     - 耳かき（掏耳）                 │
│     - 添い寝（陪睡）                 │
│     - 囁き（耳语）                   │
│     - お風呂（洗澡）                 │
│     - マッサージ（按摩）             │
│     - 催眠 / 安眠                    │
│     - 更多从DLsite自动同步...        │
│  + 自定义标签                        │
│     - 自由创建任意标签               │
│     - 标签颜色（红/蓝/绿/紫/橙等）   │
│     - 标签分组                       │
│  + 智能标签                          │
│     - 根据声优自动归类               │
│     - 根据社团自动归类               │
│     - 根据作品时长自动归类           │
└─────────────────────────────────────┘
```

---

## 5. 数据库设计

### 5.1 表结构

```sql
-- ==================== 作品表 ====================
CREATE TABLE works (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    rj_code         TEXT    NOT NULL UNIQUE,       -- RJ号 (如: RJ01014447)
    title_ja        TEXT,                           -- 日文标题
    title_zh        TEXT,                           -- 中文标题
    title_en        TEXT,                           -- 英文标题
    circle_name     TEXT,                           -- 社团名
    circle_id       TEXT,                           -- 社团ID
    cover_url       TEXT,                           -- 封面URL
    work_type       TEXT    NOT NULL,               -- 作品类型: voice/game/comic/cg
    price           INTEGER,                        -- 价格
    sale_date       TEXT,                           -- 发售日期
    description     TEXT,                           -- 简介
    age_class       TEXT,                           -- 年龄分级: general/r18/r18g
    duration_min    INTEGER,                        -- 总时长(分钟)
    file_size_mb    REAL,                           -- 文件大小(MB)
    dlsite_url      TEXT,                           -- DLsite页面链接
    created_at      TEXT    DEFAULT (datetime('now')),
    updated_at      TEXT    DEFAULT (datetime('now'))
);

-- ==================== 声优表 ====================
CREATE TABLE voice_actors (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    name            TEXT    NOT NULL UNIQUE,         -- 声优名
    dlsite_url      TEXT                             -- DLsite声优页面
);

-- ==================== 作品-声优关联表 ====================
CREATE TABLE work_actors (
    work_id         INTEGER NOT NULL,
    actor_id        INTEGER NOT NULL,
    PRIMARY KEY (work_id, actor_id),
    FOREIGN KEY (work_id) REFERENCES works(id) ON DELETE CASCADE,
    FOREIGN KEY (actor_id) REFERENCES voice_actors(id) ON DELETE CASCADE
);

-- ==================== 作品标签表 ====================
CREATE TABLE tags (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    name            TEXT    NOT NULL UNIQUE,         -- 标签名
    color           TEXT    DEFAULT '#888888',       -- 标签颜色 (HEX)
    is_preset       INTEGER DEFAULT 0               -- 是否预置标签
);

-- ==================== 作品-标签关联表 ====================
CREATE TABLE work_tags (
    work_id         INTEGER NOT NULL,
    tag_id          INTEGER NOT NULL,
    is_auto         INTEGER DEFAULT 0,              -- 是否自动生成
    PRIMARY KEY (work_id, tag_id),
    FOREIGN KEY (work_id) REFERENCES works(id) ON DELETE CASCADE,
    FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE
);

-- ==================== 账号表 ====================
CREATE TABLE accounts (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    name            TEXT    NOT NULL,                -- 账号备注名
    account_type    TEXT    DEFAULT 'dlsite',        -- 账号类型
    created_at      TEXT    DEFAULT (datetime('now'))
);

-- ==================== 作品-账号关联表（多账号去重） ====================
CREATE TABLE work_accounts (
    work_id         INTEGER NOT NULL,
    account_id      INTEGER NOT NULL,
    purchased_at    TEXT,                            -- 购买日期
    PRIMARY KEY (work_id, account_id),
    FOREIGN KEY (work_id) REFERENCES works(id) ON DELETE CASCADE,
    FOREIGN KEY (account_id) REFERENCES accounts(id) ON DELETE CASCADE
);

-- ==================== 本地文件表 ====================
CREATE TABLE local_files (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    work_id         INTEGER NOT NULL,                -- 关联作品
    local_path      TEXT    NOT NULL,                -- 本地路径
    file_type       TEXT    DEFAULT 'archive',       -- archive/dir/unknown
    download_status TEXT    DEFAULT 'not_downloaded',-- not_downloaded/downloading/downloaded
    download_progress REAL   DEFAULT 0,              -- 下载进度 0~100
    file_size       INTEGER,                         -- 文件大小(字节)
    created_at      TEXT    DEFAULT (datetime('now')),
    FOREIGN KEY (work_id) REFERENCES works(id) ON DELETE CASCADE
);

-- ==================== 音频文件表 ====================
CREATE TABLE audio_tracks (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    work_id         INTEGER NOT NULL,                -- 关联作品
    file_path       TEXT    NOT NULL,                -- 文件路径
    track_number    INTEGER,                         -- 音轨号
    title           TEXT,                            -- 音轨标题
    duration_sec    REAL,                            -- 时长(秒)
    file_format     TEXT,                            -- 格式: flac/mp3/wav/m4a
    file_size       INTEGER,                         -- 文件大小(字节)
    FOREIGN KEY (work_id) REFERENCES works(id) ON DELETE CASCADE
);

-- ==================== 播放记录表 ====================
CREATE TABLE play_history (
    id              INTEGER PRIMARY KEY AUTOINCREMENT,
    work_id         INTEGER NOT NULL,                -- 作品
    track_id        INTEGER,                         -- 音轨(可选)
    last_position   REAL    DEFAULT 0,               -- 上次播放位置(秒)
    play_count      INTEGER DEFAULT 0,               -- 播放次数
    last_played_at  TEXT,                             -- 上次播放时间
    FOREIGN KEY (work_id) REFERENCES works(id) ON DELETE CASCADE,
    FOREIGN KEY (track_id) REFERENCES audio_tracks(id) ON DELETE SET NULL
);

-- ==================== 索引 ====================
CREATE INDEX idx_works_rj ON works(rj_code);
CREATE INDEX idx_works_circle ON works(circle_name);
CREATE INDEX idx_works_type ON works(work_type);
CREATE INDEX idx_audio_work ON audio_tracks(work_id);
CREATE INDEX idx_play_history_work ON play_history(work_id);
CREATE INDEX idx_play_history_played ON play_history(last_played_at);

-- 全文搜索虚拟表
CREATE VIRTUAL TABLE works_fts USING fts5(
    rj_code, title_ja, title_zh, title_en, circle_name, description,
    content='works',
    content_rowid='id'
);
```

### 5.2 数据库关系图

```
┌──────────┐     ┌──────────────┐     ┌──────────┐
│  works   │1───N│ work_actors  │N───1│ voice_   │
│  (作品)   │     │ (作品-声优)   │     │ actors   │
└────┬─────┘     └──────────────┘     │ (声优)   │
     │                                └──────────┘
     │1            ┌──────────────┐
     ├───────────N│ work_tags    │N───1┌──────────┐
     │            │ (作品-标签)   │     │  tags    │
     │            └──────────────┘     │ (标签)   │
     │1                                └──────────┘
     ├───────────N┌──────────────┐
     │            │ work_accounts│N───1┌──────────┐
     │            │ (作品-账号)   │     │ accounts │
     │            └──────────────┘     │ (账号)   │
     │1                                └──────────┘
     ├───────────N┌──────────────┐
     │            │ local_files  │
     │            │ (本地文件)    │
     │1            └──────────────┘
     ├───────────N┌──────────────┐
     │            │audio_tracks  │
     │            │ (音频音轨)    │
     │1            └──────────────┘
     └───────────N┌──────────────┐
                  │play_history  │
                  │ (播放记录)    │
                  └──────────────┘
```

---

## 6. DLsite API 集成

### 6.1 认证方式

DLsite 使用了 **Play v3 API**，需要研究以下认证方式：

| 方式 | 难度 | 说明 |
|:---|:---:|:------|
| 🔑 **Cookie 登录** | ⭐⭐ | 手动从浏览器复制 Cookie，写入配置文件 |
| 🔐 **账号密码登录** | ⭐⭐⭐⭐ | 模拟 DLsite 登录流程，获取 Session |
| 📋 **手动导入购买记录** | ⭐ | 最保守但最稳定：用户手动从 DLsite 页面导出 |

**推荐方案：** 先做 **Cookie 登录**（最简单），后续再完善账号密码登录。

### 6.2 核心 API 端点

> 以下基于 dlsite-manager 的逆向结果，实际可能变动

```
# 获取已购作品列表
GET https://www.dlsite.com/play/api/v3/user/products
  Params: ?page=1&per_page=50&sort=purchased_at_desc

# 获取作品详情
GET https://www.dlsite.com/play/api/v3/products/{rj_code}

# 获取作品下载信息
GET https://www.dlsite.com/play/api/v3/products/{rj_code}/download

# 搜索作品
GET https://www.dlsite.com/play/api/v3/products/search
  Params: ?keyword=xxx&page=1
```

### 6.3 元数据抓取

除了 API，还可以直接解析 DLsite 作品页面的 HTML：

```
https://www.dlsite.com/maniax/work/=/product_id/{rj_code}.html
https://www.dlsite.com/home/work/=/product_id/{rj_code}.html  (全年齢)
```

需要抓取的数据：
- 作品标题（多语言）
- 封面图片
- 社团信息
- 声优列表
- 标签/分类
- 作品简介
- 文件列表/大小
- 价格/发售日

### 6.4 下载流程

```
1. 获取作品下载信息 → 得到文件列表和下载URL
2. 发起 HTTP GET 请求下载压缩包（支持 Range 头断点续传）
3. 下载到临时目录（staging）
4. 校验文件完整性
5. 解压到目标目录
6. 扫描解压后的音频文件，写入 audio_tracks 表
7. 更新 local_files 表状态为 downloaded
```

---

## 7. UI/UX 设计

### 7.1 整体布局

```
┌──────────────────────────────────────────────────────────┐
│  🎧 DLsite Manager                    [设置] [账号] [≡]  │  ← 顶栏
├──────────┬───────────────────────────────────────────────┤
│          │                                                │
│  📚 全部  │   ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐            │
│  ⬇️ 已下载 │   │ 封面 │ │ 封面 │ │ 封面 │ │ 封面 │ │ 封面 │            │
│  📥 下载中 │   └───┘ └───┘ └───┘ └───┘ └───┘            │
│  🏷️ 标签   │   RJ010144  RJ010255  RJ010368  ...        │
│  ├───────  │  社团:A    社团:B    社团:C                  │
│  │ 掏耳   │                                                │
│  │ 陪睡   │   ┌───┐ ┌───┐ ┌───┐ ┌───┐ ┌───┐            │
│  │ 耳语   │   │ 封面 │ │ 封面 │ │ 封面 │ │ 封面 │ │ 封面 │            │
│  └───────  │   └───┘ └───┘ └───┘ └───┘ └───┘            │
│            │                                                │
│  🔍        │   [← 1 2 3 4 5 ... →]                       │  ← 分页
├──────────┴───────────────────────────────────────────────┤
│  播放器迷你栏: ⏮ ⏸ ⏭  [████████░░░░░░] 03:25 / 15:30  │  ← 底栏
└──────────────────────────────────────────────────────────┘
```

### 7.2 关键页面设计

#### 页面1：作品库（主页面）

```
┌─────────────────────────────────────────────────────────┐
│  🔍 [搜索作品/RJ号...]         [网格视图▼] [排序▼]   │
│                                                          │
│  筛选栏: [全部] [已下载] [未下载] [下载中]              │
│                                                          │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐               │
│  │      │  │      │  │      │  │      │               │
│  │ 封面  │  │ 封面  │  │ 封面  │  │ 封面  │               │
│  │      │  │      │  │      │  │      │               │
│  │ 标题  │  │ 标题  │  │ 标题  │  │ 标题  │               │
│  │ 社团  │  │ 社团  │  │ 社团  │  │ 社团  │               │
│  │ ⬇️✓   │  │ ⬇️    │  │ ⬇️✓   │  │ ⬇️▶   │               │
│  └──────┘  └──────┘  └──────┘  └──────┘               │
│                                                          │
│  ┌──────┐  ┌──────┐  ┌──────┐                          │
│  │ 封面  │  │ 封面  │  │ 封面  │                          │
│  │ ...  │  │ ...  │  │ ...  │                          │
│  └──────┘  └──────┘  └──────┘                          │
└─────────────────────────────────────────────────────────┘
```

#### 页面2：作品详情（点击作品后）

```
┌─────────────────────────────────────────────────────────┐
│  ← 返回作品库                                            │
│                                                          │
│  ┌──────────────────────┐  ┌──────────────────────────┐ │
│  │                      │  │  RJ01014447              │ │
│  │     封面大图          │  │  📋 タイトル              │ │
│  │                      │  │  【寝落ちASMR】悪魔娘が...│ │
│  │                      │  │                           │ │
│  │                      │  │  🏢 社团: サークル名      │ │
│  │                      │  │  🎤 声優: 佐倉綾音       │ │
│  │                      │  │  📅 发售: 2024-12-31     │ │
│  │                      │  │  💰 价格: 1,320円        │ │
│  │                      │  │  ⏱ 时长: 約120分         │ │
│  │                      │  │  🏷️ 标签: #掏耳 #陪睡    │ │
│  │                      │  │                           │ │
│  │                      │  │  [⬇️ 下载] [▶️ 播放]      │ │
│  │                      │  │  [📂 打开文件夹] [🌐 打开DLsite]│ │
│  └──────────────────────┘  └──────────────────────────┘ │
│                                                          │
│  📝 简介:                                                │
│  ┌──────────────────────────────────────────────────────┐│
│  │ 悪魔娘が疲れたあなたを癒してくれるASMR...           ││
│  └──────────────────────────────────────────────────────┘│
│                                                          │
│  🎵 音轨列表:                                           │
│  ┌──────────────────────────────────────────────────────┐│
│  │ ▶ 01. プロローグ                     05:23           ││
│  │    02. 耳かき（右耳）                15:45           ││
│  │    03. 耳かき（左耳）                14:30           ││
│  │    04. 添い寝                        30:00           ││
│  │    ...                                               ││
│  └──────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────┘
```

#### 页面3：播放器（点击播放后展开）

```
┌─────────────────────────────────────────────────────────┐
│  ← 返回详情                                              │
│                                                          │
│                    ┌──────────────┐                      │
│                    │              │                      │
│                    │   封面大图    │                      │
│                    │  (封面艺术)   │                      │
│                    │              │                      │
│                    └──────────────┘                      │
│                                                          │
│            【寝落ちASMR】悪魔娘が最高に癒す...           │
│                    01. 耳かき（右耳）                    │
│                                                          │
│       ⏮      ⏸      ⏭      🔁                          │
│                                                          │
│       03:25 ───●─────────────── 15:30                   │
│                                                          │
│       🔊 ────●─────── 🔇       1.0x ▼                   │
│                                                          │
│       📋 播放列表:                                       │
│       ┌──────────────────────────────────────────────┐  │
│       │ ▶ 01. プロローグ                 05:23       │  │
│       │    02. 耳かき（右耳）            15:45       │  │
│       │    03. 耳かき（左耳）            14:30       │  │
│       │    04. 添い寝                    30:00       │  │
│       └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 7.3 设计风格

| 项目 | 推荐 |
|:---|:-----|
| 🎨 **主题** | 深色模式为主（ASMR 场景多为夜间使用） |
| 🌈 **配色** | 主色：#1a1a2e（深蓝紫），辅色：#e94560（珊瑚红），强调：#0f3460 |
| 🔊 **音频可视化** | 波形图配色跟随封面主色提取 |
| 🐱 **彩蛋** | 加载动画可以是一只小猫咪摇尾巴喵~ |

---

## 8. 开发路线图

### Phase 1：MVP（最小可用产品）— 预计2周

```
目标：能跑起来，能看作品列表，能下载

第1天  搭建 Tauri 项目骨架 + 前端基础框架
第2天  SQLite 数据库建表 + 基础 CRUD
第3-4天  DLsite API 集成：登录 + 同步已购列表
第5天  作品库页面：网格视图 + 搜索筛选
第6-7天  作品详情页
第8-10天 下载引擎：下载 + 断点续传 + 解压
第11天  下载队列管理页面
第12天  设置页面 + 账号管理
第13-14天 测试 + 修复 Bug
```

### Phase 2：核心体验 — 预计1周

```
目标：能听 ASMR 了

第1-2天  音频播放器核心（播放/暂停/进度/音量）
第3天  播放列表 + 音轨管理
第4天  播放进度记忆 + 继续播放
第5天  迷你模式 + 全局快捷键
第6天  本地文件夹扫描 + 导入
第7天  测试 + 优化
```

### Phase 3：进阶功能 — 预计1周

```
目标：更好用，更完善

第1天  标签系统（CRUD + 批量操作）
第2天  多账号管理 + 去重
第3天  音频可视化（波形/频谱）
第4天  倍速播放 + 均衡器
第5天  智能标签自动分类
第6天  数据导出/导入（备份）
第7天  性能优化 + 测试
```

### Phase 4：完善与发布 — 持续

```
目标：打磨细节，发布

- 深色/浅色主题切换
- 多语言（中文/日文/英文）
- 自动更新机制
- 打包分发（AppImage / .deb / .dmg）
- 用户反馈收集
```

---

## 9. 部署与打包

### 9.1 开发环境搭建（Windows）

```powershell
# ===== 1. 安装 Rust =====
# 下载 rustup-init.exe 并运行
# https://win.rustup.rs/x86_64
# 选择 1) Default installation，一路回车
# 安装完成后重启终端，验证：
rustc --version        # 应该看到 rustc 1.xx.x
cargo --version

# ===== 2. 安装 Node.js =====
# 去 https://nodejs.org 下载 LTS 版本
# 安装时勾选 "Add to PATH"
node --version
npm --version

# ===== 3. 安装 Visual Studio Build Tools =====
# 下载 Visual Studio 2022 Build Tools:
# https://visualstudio.microsoft.com/visual-cpp-build-tools/
# 安装时勾选 "使用C++的桌面开发" 工作负载
# 这是 Rust 编译 C 代码必需的环境

# ===== 4. 创建项目 =====
npm create tauri-app@latest dlsite-asmr-manager
# 选择前端框架 (推荐 Vue 3 或 Svelte)

# ===== 5. 安装依赖 =====
cd dlsite-asmr-manager
npm install

# 添加 Rust 后端依赖
cargo add rusqlite reqwest tokio serde serde_json
cargo add tauri-plugin-shell tauri-plugin-dialog
```

### 9.2 项目目录结构

```
dlsite-asmr-manager/
├── src/                     # 前端代码 (Svelte/Vue)
│   ├── lib/
│   │   ├── components/     # 组件
│   │   │   ├── WorksGrid.svelte    # 作品网格
│   │   │   ├── WorksList.svelte    # 作品列表
│   │   │   ├── WorkCard.svelte     # 作品卡片
│   │   │   ├── SearchBar.svelte    # 搜索栏
│   │   │   ├── FilterPanel.svelte  # 筛选面板
│   │   │   ├── AudioPlayer.svelte  # 音频播放器
│   │   │   ├── DownloadQueue.svelte# 下载队列
│   │   │   └── TagEditor.svelte    # 标签编辑器
│   │   ├── stores/         # 状态管理
│   │   │   ├── works.ts           # 作品状态
│   │   │   ├── player.ts          # 播放器状态
│   │   │   └── download.ts        # 下载状态
│   │   └── utils/          # 工具函数
│   │       ├── api.ts             # 调用 Tauri 命令
│   │       └── format.ts          # 格式化
│   ├── routes/             # 页面路由
│   │   ├── +page.svelte           # 作品库主页
│   │   ├── work/[id]/+page.svelte # 作品详情
│   │   ├── player/+page.svelte    # 播放器
│   │   ├── download/+page.svelte  # 下载管理
│   │   └── settings/+page.svelte  # 设置
│   └── app.html
├── src-tauri/              # Rust 后端
│   ├── src/
│   │   ├── main.rs              # 入口
│   │   ├── lib.rs               # 库
│   │   ├── commands/            # Tauri 命令
│   │   │   ├── mod.rs
│   │   │   ├── works.rs         # 作品相关命令
│   │   │   ├── download.rs      # 下载相关命令
│   │   │   ├── player.rs        # 播放相关命令
│   │   │   ├── accounts.rs      # 账号相关命令
│   │   │   └── tags.rs          # 标签相关命令
│   │   ├── db/                  # 数据库
│   │   │   ├── mod.rs
│   │   │   ├── models.rs        # 数据模型
│   │   │   ├── schema.rs        # 表结构
│   │   │   └── migrations/      # 数据库迁移
│   │   ├── api/                 # DLsite API
│   │   │   ├── mod.rs
│   │   │   ├── auth.rs          # 认证
│   │   │   ├── products.rs      # 作品API
│   │   │   └── download.rs      # 下载API
│   │   └── audio/               # 音频处理
│   │       ├── mod.rs
│   │       └── player.rs        # 播放引擎
│   ├── Cargo.toml
│   └── tauri.conf.json
├── package.json
└── README.md
```

### 9.3 打包发布（Windows）

```powershell
# 开发模式（热更新）
npm run tauri dev

# 构建发布包
npm run tauri build

# 输出目录（Windows 下生成 .msi 安装包）
# src-tauri/target/release/bundle/
#   ├── msi/         # 安装包 (.msi) — 推荐，自动安装
#   └── nsis/        # 安装包 (.exe) — 传统安装器
#
# 打包后直接双击 .msi 安装即可

# 如果需要生成便携版（免安装）：
# 在 tauri.conf.json 中配置：
# "bundle": {
#   "windows": {
#     "wix": { ... }  # 或使用 nsis
#   }
# }
```

> 💡 **小提示**：Windows 上构建时，杀毒软件可能会误报 Rust 编译的 exe，
> 可以在 tauri.conf.json 中给应用添加数字签名，或者直接添加白名单喵~

---

## 附录 A：参考项目

| 项目 | 可借鉴的点 |
|:----|:----------|
| [dlsite-manager](https://github.com/AcrylicShrimp/dlsite-manager) | Tauri+Rust+Svelte 架构，DLsite API 逆向思路 |
| [dlsite-asmr-player](https://github.com/ozonezone/dlsite-asmr-player) | Docker 部署，自动扫描 RJ 号文件夹 |
| [dvtag](https://github.com/NobeKanai/dvtag) | 同人音声打标签 CLI 工具，元数据抓取思路 |
| [Bakabase](https://github.com/anobaka/Bakabase) | 二次元本地媒体管理器，UI 设计参考 |
| [EaraAsmrPlayer](https://github.com/eValDoll/EaraAsmrPlayer) | Android ASMR 播放器，播放器交互设计参考 |

## 附录 B：小玥喵的私房建议 🐱

### 先做啥，后做啥？

```
第1步：搭好 Tauri 壳子 + SQLite 数据库
第2步：做一个「手动导入」功能（用户自己填RJ号，你自动抓元数据）
      → 先不用搞登录，降低复杂度
第3步：作品库浏览 + 搜索
第4步：下载功能
第5步：播放器
第6步：再慢慢加登录同步、标签、可视化...

这样每一步都能看到成果，不会写到一半没动力喵~！
```

### 技术难度排序（从易到难）

```
⭐  手动导入作品（填RJ号抓元数据）
⭐⭐ 本地文件夹扫描（识别RJ号）
⭐⭐ 作品库展示 + 搜索筛选
⭐⭐⭐ 下载引擎（断点续传）
⭐⭐⭐ 音频播放器（基本播放）
⭐⭐⭐⭐ DLsite 账号登录同步
⭐⭐⭐⭐ 标签系统
⭐⭐⭐⭐⭐ 音频可视化 + 均衡器
```

**先从⭐的开始做，建立信心喵~！** 🐱✨

---

> 🎯 **设计书到此结束**  
> 有任何问题随时问玥玥喵~！❤️